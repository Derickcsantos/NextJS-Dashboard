/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {},
  reactStrictMode: true,
  // ❌ remove a linha abaixo:
  // output: 'standalone'
};

export default nextConfig;
